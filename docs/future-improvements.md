# Future Improvements

1. Deploy to staging and deploy to production workflows need to be added
2. Future workflows can be chained. e.g. deploy to staging runs once deploy to dev finishes
3. A notification system can be added to notify the team when deploy to dev passes successfully
4. Security scans can be added to CI
5. Hotfix pipeline can be added for emergency fixes that bypasses the multiple checks and steps for immediate error fixes.
